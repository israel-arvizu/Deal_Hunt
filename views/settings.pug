extends layout.pug

block content
    if errors.length > 0
        each err in errors
            p= err
    form(action="/settings" method="post")
        input(type="hidden" name="_csrf" value=csrfToken)
        label(for="firstName") First Name
        input(type="text" name="firstName" required value=loggedInUser.firstName)
        label(for="lastName") Last Name
        input(type="text" name="lastName" required value=loggedInUser.lastName)
        //- label(for="email") Email
        //- input(type="text" name="email" required value=user.email)
        button(type='submit') Save Changes

        //- button(type='button' class= 'delete_profile' id=`${user.id}`) Delete Profile
